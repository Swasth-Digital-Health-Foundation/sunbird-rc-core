{
  "$schema": "http://json-schema.org/draft-07/schema",
  "type": "object",
  "properties": {
    "Organisation_Audit": {
      "$ref": "#/definitions/Organisation_Audit"
    }
  },
  "required": [
    "Organisation_Audit"
  ],
  "title":"Organisation_Audit",
  "definitions": {
    "Organisation_Audit": {
      "$id": "#/properties/Organisation_Audit",
      "type": "object",
      "title": "The Test Schema",
      "required": [
        "recordId",
        "transactionId",
        "auditJson",
        "action",
        "userId",
        "timestamp"
      ],
      "properties": {
        "recordId": {
          "$id": "#/properties/recordId",
          "type": "string",
          "$comment": "Record ID of the entity"
        },
        "transactionId": {
          "$id": "#/properties/transactionId",
          "type": "array",
          "$comment": "transaction id of the commit"
        },
        "auditJson": {
          "$id": "#/properties/auditJson",
          "type": "string",
          "$comment": "Json difference after update"
        },
        "action": {
          "$id": "#/properties/action",
          "type": "string",
          "$comment": "Type of action performed on entity read, update, add etc .."
        },
        "userId": {
          "$id": "#/properties/userId",
          "type": "string",
          "$comment": "user information who performed operation on the entity"
        },
        "timestamp": {
          "$id": "#/properties/timestamp",
          "type": "string",
          "$comment": "Timestamp",
          "default": "",
          "examples": [
            "1580729777849"
          ]
        }
      }
    }
  },
  "_osConfig": {
    "systemFields": ["osCreatedAt", "osUpdatedAt"]

  }
}